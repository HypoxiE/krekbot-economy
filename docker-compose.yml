
services:
  discord-economy-bot:
    build: .
    image: discord-economy-bot
    container_name: discord-economy-bot
    restart: always
    networks:
      - postgres-network
    env_file:
      - .env

networks:
  postgres-network:
    external: true
